
<h1 class="display-3 text-center my-3">Citations</h1>
<div class="container">
<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="light">
    <div class="container-fluid">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <a class="nav-link" href="{{ path('citations') }}">Les Citations
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('auteur') }}">Les Auteurs
            </a>
          </li>
          {# Verifi si l'utilisateur a un role admin #}
          {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('user') }}">Les Utilisateurs
            </a>
          </li>
          {% endif %}
        </ul>
        <ul class="navbar-nav ms-auto">
          {% if is_granted('IS_AUTHENTICATED_FULLY') %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('security.logout') }}">Déconnexion
            </a>
          </li>
          {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('security.login') }}">Connexion
            </a>
          </li>
          {% endif %}
 
      </div>
    </div>
  </nav>
</div>