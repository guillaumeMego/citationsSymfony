<h1 class="display-2 my-3 text-center">Citations</h1>
<div class="container">
<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('citations') }}">Les Citations
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('auteur') }}">Les Auteurs
          </a>
        </li>
        {# Verifi si l'utilisateur a un role admin #}
        {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('user') }}">Les Utilisateurs
          </a>
        </li>
        {% endif %}
      </ul>
      <ul class="navbar-nav ms-auto">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('security.logout') }}">Déconnexion
          </a>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('security.login') }}">Connexion
          </a>
        </li>
        {% endif %}
      </ul>
      
    </div>
  </div>
</nav>

</div>













